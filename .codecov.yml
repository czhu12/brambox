coverage:
  precision: 2
  round: down
  range: "50...100"

  status:
    project:
      default:
        threshold: 5
    patch:
      default: off
    changes:
      default: off

comment:
  layout: "header, reach, diff, flags, files, footer"
  behavior: default
  require_changes: no
  require_base: no
  require_head: yes
